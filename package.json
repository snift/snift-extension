{
  "name": "snift",
  "version": "0.0.1",
  "description": "Measuring the web's security.",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build",
    "clean": "rm -rf ./dist/*"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  },
  "dependencies": {
    "axios": "^0.18.1",
    "is-empty": "^1.2.0"
  }
}
